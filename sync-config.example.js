/* Optional API configuration for a deployed P10354 server.
   Copy this file to sync-config.js and set the same API base/token used by the server.
   When the browser and API share the same server, apiBase can remain "/api". */
window.P10354_SYNC_CONFIG = {
  apiBase: "/api",
  token: "",
};
